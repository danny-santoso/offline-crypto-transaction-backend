{
  "name": "offline-crypto-backend",
  "version": "1.0.0",
  "description": "Backend API for offline cryptocurrency transactions",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "deploy:local": "cd blockchain && npx hardhat run scripts/deploy.ts --network localhost",
    "deploy:sepolia": "cd blockchain && npx hardhat run scripts/deploy.ts --network sepolia",
    "deploy:goerli": "cd blockchain && npx hardhat run scripts/deploy.ts --network goerli",
    "verify:sepolia": "cd blockchain && npx hardhat run scripts/verify.ts --network sepolia",
    "verify:goerli": "cd blockchain && npx hardhat run scripts/verify.ts --network goerli",
    "setup-otm:local": "cd blockchain && npx hardhat run scripts/setup-otm.ts --network localhost",
    "setup-otm:sepolia": "cd blockchain && npx hardhat run scripts/setup-otm.ts --network sepolia",
    "setup-otm:goerli": "cd blockchain && npx hardhat run scripts/setup-otm.ts --network goerli",
    "blockchain:compile": "cd blockchain && npx hardhat compile",
    "blockchain:test": "cd blockchain && npx hardhat test",
    "blockchain:node": "cd blockchain && npx hardhat node",
    "test:pipeline": "ts-node scripts/test-pipeline.ts",
    "test:pipeline:quick": "ts-node scripts/test-pipeline.ts --quick",
    "test:integration": "jest --testPathPattern=integration --runInBand",
    "test:unit": "jest --testPathPattern=src/__tests__/(?!integration)",
    "test:e2e": "jest --testPathPattern=tokenLifecycle --runInBand",
    "test:performance": "jest --testPathPattern=apiPerformance --runInBand"
  },
  "keywords": [
    "cryptocurrency",
    "blockchain",
    "offline",
    "ethereum"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.0",
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.8",
    "@types/node": "^20.9.0",
    "@types/supertest": "^6.0.3",
    "@typescript-eslint/eslint-plugin": "^6.12.0",
    "@typescript-eslint/parser": "^6.12.0",
    "eslint": "^8.54.0",
    "ethers": "^6.15.0",
    "hardhat": "^2.26.0",
    "jest": "^29.7.0",
    "supertest": "^7.1.3",
    "ts-jest": "^29.1.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "web3": "^4.2.2"
  }
}
